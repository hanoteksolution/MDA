# Shared

Cross-cutting constants and schemas shared between backend, frontend, and desktop.

## Contents

- `constants/` — Module names, role definitions, permission keys, document statuses
- `schemas/` — JSON Schema or OpenAPI fragments for API contracts

Keeps frontend types, backend validation, and sync payloads aligned.
