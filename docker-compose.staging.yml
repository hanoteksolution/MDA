# Staging / local Docker overrides — expose DB + API for debugging
name: mda

services:
  db:
    ports:
      - "127.0.0.1:5432:5432"

  api:
    ports:
      - "127.0.0.1:8000:8000"

  web:
    ports:
      - "127.0.0.1:8010:80"
